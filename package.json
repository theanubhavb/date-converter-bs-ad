{
  "name": "date-converter-bs-ad",
  "version": "1.0.0",
  "private": true,
  "description": "Excel custom functions for Gregorian (AD) and Bikram Sambat (BS) dates.",
  "scripts": {
    "build": "webpack --mode production",
    "start": "npx serve dist -l 3000",
    "start:desktop": "npx serve dist -l 3000",
    "start:server": "webpack serve --mode development",
    "stop": "echo 'No local Office sideload needed; use the published GitHub Pages manifest.'",
    "validate": "office-addin-manifest validate manifest.xml && npm run build",
    "validate:github-pages": "office-addin-manifest validate manifest.xml && npm run build",
    "test": "jest",
    "lint": "eslint .",
    "configure:github-pages": "node scripts/configure-github-pages.js",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "copy-webpack-plugin": "latest",
    "custom-functions-metadata-plugin": "latest",
    "eslint": "latest",
    "gh-pages": "^6.3.0",
    "html-webpack-plugin": "latest",
    "jest": "latest",
    "office-addin-debugging": "latest",
    "office-addin-dev-certs": "latest",
    "office-addin-manifest": "latest",
    "typescript": "~5.8.3",
    "webpack": "latest",
    "webpack-cli": "latest",
    "webpack-dev-server": "latest"
  }
}
